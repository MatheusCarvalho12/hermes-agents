---
name: kanban-orchestration
description: "Orquestrar time via kanban: tasks, monitor, recovery."
version: 1.0.0
author: Hermes Agent
license: MIT
platforms: [macos, linux]
metadata:
  hermes:
    tags: [kanban, orchestration, multi-profile, delegation, merge]
    related_skills: [hermes-multi-profile-ops, github-pr-workflow, i-have-adhd]
---

# Kanban Orchestration

Fluxo operacional do ORQUESTRADOR sobre o kanban do Hermes, validado em entrega real multi-perfil (repo datalake-mega, 10 tasks, 2 demandas). Complementa a skill `hermes-multi-profile-ops` (do usuário: gestão de perfis/SOUL/skills + inventário do time); esta carrega o padrão de orquestração: criar tasks, dependências, monitoramento async, recovery de crash e merge.

## Regra transversal: edição de doc de agente
ANTES de qualquer `skill_manage` (patch/edit/write_file) em SKILL.md, SOUL.md ou AGENTS.md (incluindo scripts/ e references/ de skills): carregar `writing-for-agents` PRIMEIRO e seguir os princípios (gatilho na description, positive framing, sem data stale, co-location). A regra mora AQUI porque o SOUL.md sozinho não dispara de forma confiável (contexto cheio + gatilho implícito) — esta skill é carregada em toda orquestração, então o check acontece no ponto de ação. Juntar `hermes-agent-skill-authoring` (formato válido) quando a edição for de skill.

## Regra transversal: demanda nova do usuário → ask-matt primeiro
Toda vez que o usuário abrir demanda nova (feature, plano, mudança): o PRIMEIRO tool call é `skill_view(name='ask-matt')` e seguir o fluxo que ela aponta (grill-with-docs → to-spec → to-tickets → implement). Medição de aderência: `~/.hermes/skills/.usage.json` (view_count da ask-matt/grilling/to-spec/to-tickets cresce a cada demanda). Exceção explícita do SOUL: usuário pediu velocidade ("faz rápido") → executa direto.

## Regra transversal: Context7 nos workers
Todo body de task que envolva API/lib (backend/frontend/database): seção "Context7 EVIDENCE: cite no summary qual lib+versão o Context7 confirmou (query-docs). Sem citação = devolver." — exigir evidência no ponto de ação (o body), não só no SOUL do worker. Verificar aderência na revisão pós-done (grep por 'context7' no summary/kanban log).

## Quando usar
- Delegar features para os perfis via kanban (backend/frontend/database/designer)
- Acompanhar N workers em paralelo sem gastar turnos com polling
- Task de worker que crashou no protocolo ou se auto-bloqueou pedindo revisão

## Fluxo (resumo)
1. Entender demanda → quebrar em tasks ENXUTAS (título + o que + criterio de pronto). Feature ambígua: no maximo 1 pergunta.
2. Criar tasks: `--workspace "dir:<abs-repo>"` (scratch é efêmero e some ao completar), `--priority <int>`, `--parent <id>` repetível para dependências.
3. DEFINIR CONTRATO nos bodies (schema SQL, shape JSON da API, nomes de tabela) → database/backend/frontend rodam em PARALELO sem pisar um no outro.
4. Monitorar com script background + `notify_on_complete=true` — NUNCA polling com sleep (usuario corrigiu explicitamente: quer notificação na hora exata).
5. Quando fechar: VERIFICAR entrega real (arquivos existem, build/testes rodam, log mostra sucesso) — nunca confiar em self-report.
6. Revisar auto-blocks, completar, rodar verificacao unica pre-PR (pytest + ruff + review), commit, PR, merge squash + limpeza de branch.

## Orquestrador verifica e delega — NUNCA desenvolve (correção explícita 2026-08-06)
Ao iniciar módulo novo, o usuário pode pedir "revisa a base primeiro e traz veredito antes de delegar": o orquestrador VALIDA empiricamente (worktree da branch + `alembic upgrade head` + pytest) e monta o veredito com evidência — mas TODA implementação que a revisão revelar vira task no kanban para o perfil dono (inclusive correções pequenas). O usuário corrigiu no meio da sessão: "tudo isso que tu for desenvolver não é para tu desenvolver — é para repassar para os agentes pelo kanban". Limite prático: orquestrador não faz auditoria profunda de código (grep/leitura de application/api além do necessário pro veredito); achados de review entram no body da task como seção "Achado do review" com critério de pronto.
- Padrão "orquestração separada por módulo" (2026-08-06, módulo documentos): worktree dedicado (`git worktree add -b <branch> <path> origin/<branch>`) + cadeia de tasks com `--parent` encadeado e `--workspace "dir:<worktree>"` → zero conflito com sessões paralelas rodando no main/outros worktrees. Verificar a branch remota antes de criar worktree (pode já existir base de sessão anterior — `git log main..origin/<branch>` + `git diff --stat main...origin/<branch>`).
- **Trabalho de REPO também é do worker — não do orquestrador (correção 2026-08-06, merge flowmex)**: usuário pegou o orquestrador fazendo TUDO na mão no fechamento de um merge (commits de trabalho solto, resolução de 3 conflitos, `alembic merge` de revisões, suíte completa, push) e corrigiu em tom forte: "por que tu não tá mandando pro back-end fazer? Tu tá fazendo na mão, que é errado". NÃO são do orquestrador, mesmo em fechamento de merge já em andamento: `git commit`, resolução de conflito de merge, merge, revisão/merge de migração alembic, push. O orquestrador VALIDA empiricamente (worktree + upgrade + pytest) e DELEGA o fechamento via kanban com body contendo: estado exato (commits/hashes), escopo autorizado (arquivos), comandos reais prontos e critério de pronto — como na task de fechamento do merge flowmex (body com `env -u FLOWMEX_DATABASE_URL` explícito). Velocidade pedida ("faz logo") = ticket enxuto com estado pronto-para-fechar, NUNCA execução manual.
- Fase de pesquisa de integração externa (ex.: Mainô → Flowmax): orquestrador entrega `docs/research-*.md` (mapa da API oficial + inventário de uso real das contas + design plug-and-play) e depois tickets verticais com contrato (adaptador → schema → endpoints+jobs → spec design → frontend). Técnica completa em `saas-integration-research`.
- **Diagnóstico de integração e correção de DADOS também são do worker (correção 2026-08-07, boleto PlugBoleto)**: usuário pegou o orquestrador lendo código do provider e fazendo UPDATE no banco de produção na mão e corrigiu: "tu que ta codando??? porque nao jogou para os profiles? ... se for 2 vai junto e tal". Limite: o orquestrador pode mapear a causa com 1-2 greps (o suficiente para escrever o contrato), mas correção de DADOS (UPDATE/estado no banco) → task para database-developer; investigação/fix de código de integração → task para backend; demandas relacionadas descobertas no meio do diagnóstico → SEGUNDA task no MESMO workspace e MESMO worker (padrão "vai junto"), nunca resolver na mão.

## Pitfalls reais do CLI kanban
- `--priority` é INT (10/5), nao string: `--priority high` → `invalid int value` (erro vai pro stderr — nao suprimir stderr ao debugar).
- `create` NÃO tem flag `--json`: capturar o ID no stdout com `grep -o 't_[a-f0-9]*'` (output "Created t_XXXX").
- Warning "No gateway is running" no create pode ser FALSO ALARME com gateway vivo — conferir `hermes gateway status` + `ps aux | grep gateway` antes de agir.
- `--body` com caracteres especiais/acentos: gravar em arquivo e usar `--body "$(cat /tmp/body.md)"` — evita quoting quebrado. **ATENÇÃO (2026-08-06): o parser hardline do terminal BLOQUEIA qualquer comando inline que contenha `$(...)`** (erro "command parser limit or malformed executable payload"; vale até para `$(grep ...)` curto, e para body literal inline >~2KB). Padrão validado: escrever `/tmp/create_tN.sh` com o `hermes kanban create ... --body "$(cat /tmp/body_tN.md)"` DENTRO do script e rodar `bash /tmp/create_tN.sh` — o parser só vê o comando curto. Capturar ID no stdout do script com `grep -o 't_[a-f0-9]*'`.
- `title` é POSICIONAL no `kanban create`: `hermes kanban create 'Título' --assignee ...` — usar `--title` dá `hermes: error: unrecognized arguments: --title` e a task nem é criada (testar create pequeno antes de montar a cadeia toda).
- Workspace `dir:` compartilha o MESMO diretório físico: tasks que editam o MESMO arquivo DEVEM ser serializadas (filha com `--parent` dos dois) — senao conflito garantido.
- **Renomear/mover worktree com tasks ativas** (2026-08-06): tasks já criadas guardam o caminho absoluto (`dir:/caminho`); mover a pasta quebra workers running E as tasks pendentes (workspace não existe mais). Se o usuário pedir rename no meio do fluxo: `git worktree move <old> <new>` + **symlink temporário** `<old> -> <new>` até TODAS as tasks do board fecharem; depois remover o link. Conferir `hermes kanban list` ANTES (workers running?) — se ainda nem começaram, mover é seguro sem symlink, mas o link não custa nada.
- Dispatcher tick ~60s: task ready → running em ~1 min.

## Escopo vazado = o time-killer #1 de worker (2026-08-05, task StatusBadge)Worker de frontend gastou 22 min numa task de 1 componente e reescreveu 20 arquivos fora do escopo (1180 linhas não commitadas: App/Header/StateViews/index.css...). Commit dele era limpo, mas o working tree virou um refactor inteiro que ninguém pediu.
- **Task de PROVA deve ser do TAMANHO REAL da demanda do usuário** (correção explícita 2026-08-05): ele rejeitou micro-task de teste ("StatusBadge porra, minhas tasks não vão ser uma coisa pequena assim"). Teste pequeno valida gatilho/processo; teste GRANDE (ex: refatorar o dashboard inteiro mantendo lógica de negócio, com spec do designer como contrato) valida escopo/autonomia/entrega. MAS: task gigante monolítica em UM ticket = contexto incha = API cai = protocol violation (ver seção protocol_violation) — a solução é quebrar a demanda grande em tickets verticais (to-tickets), não evitar tarefa grande.
- **Prevenção no body da task**: seção explícita "Escopo autorizado: <paths>" + "PROIBIDO tocar: <paths>" + "Se precisar de algo fora, pare e pergunte via kanban". Tasks GRANDES autorizam a área inteira (ex: "frontend/ inteiro") mas proíbem as outras (backend/, tests/).
- **Detecção pós-run**: `git status --short` + `git diff --stat HEAD` — se aparecer diff fora do que a task autorizou, é escopo vazado (devolver / reverter / reaproveitar com critério).
- **Time-box**: worker deve ler ≤ 30 min antes de codar em task grande; ≤ 10 min em task pequena.
- **Reaproveitar trabalho parcial de run crashado**: run anterior crashou por protocol_violation mas deixou refactor útil no working tree → orquestrador `git stash push -u -m "wip: ..."` ANTES de delegar de novo (preserva, reversível), commitar specs/contratos do designer separadamente (`git add docs/... && git commit`), e o worker novo valida o stash como baseline (build/lint) antes de decidir aproveitar ou refazer.

## protocol_violation (padrão recorrente em sessões longas)
Worker termina rc=0 SEM chamar kanban_complete/kanban_block → dispatcher marca gave_up/blocked apos 2-3 crashes; o erro instrui: "verify it and report the result via kanban_complete".

**FIX NATIVO NO UPSTREAM (Hermes v0.20.0+):** o auto-complete em finalização normal (#27199, `_kanban_stop_synthetic` no run_agent.py) faz o worker que termina com texto receber nudge sintético em vez de protocol_violation; o erro genérico ganhou fix que mostra o erro real (#46985). Protocol_violation em versão nova = run morto por API (503/ReadTimeout com contexto inchado) na maioria dos casos — seguir a seção Diagnóstico abaixo: ler o FINAL do log antes de concluir qualquer coisa.

Recovery (do orquestrador):
1. Verificar a ENTREGA REAL (log do worker mostra sucesso? build/testes rodam? arquivos existem?).
2. `hermes kanban comment` explicando a revisão + `hermes kanban complete <id>`.
3. Prevenção: encerrar o body com "CHAME kanban_complete OBRIGATORIAMENTE ao terminar (senao o dispatcher conta como crash)".

**NOTIFICAÇÃO EM TEMPO REAL (nativa, doc oficial):** `hermes kanban notify-subscribe <id> --platform <p> --chat-id <n>` — o notifier do gateway (ativo com `hermes gateway status`) entrega 1 mensagem por evento terminal (completed/blocked/gave_up/crashed/timed_out) + 1ª linha do `--result`; `/kanban create` no gateway assina automaticamente. Desktop sem gateway-chat: `scripts/watch-kanban-tasks.sh` em background + `notify_on_complete=true` — o script reconhece done/blocked/gave_up/crashed/archived como terminais.

### Diagnóstico: checar a API do modelo ANTES de culpar o worker (validado 2026-08-05, task t_b6e8e378)
O worker "não fecha o protocolo" NEM SEMPRE é desobediência — pode ser o run morrendo ANTES de chegar ao kanban_complete. Antes de concluir protocol_violation, ler o FINAL do log: `hermes kanban log <id> 2>&1 | tail -60 | grep -vE heartbeat` e procurar:
- `API call failed ... HTTP 503` (capacity limits), `stream drop (ReadTimeout) after 600s`, `Final error: Connection error`.
Caso real: task GRANDE crashou 4x com rc=0 — o worker tinha plano explícito de "commitar e fechar", mas a API do modelo caiu (503 + timeout 10min + connection) quando o contexto inchou a ~57k tokens / 40 msgs num run de 22min. Não era o worker nem o SOUL — era infra do modelo.
- Correção estrutural: **sizear a task para caber numa janela de contexto fresca** — tasks verticais pequenas (skill `to-tickets` do mattpocock: tracer-bullet slices com bloqueios) fecham o protocolo de forma confiável; task monolítica gigante + modelo instável = morte certa.
- **VALIDADO na cadeia T1→T2→T3 (2026-08-05)**: quebrar a demanda grande em 3 tickets verticais com `--parent` encadeado fez cada um fechar `done` em ~3 min, protocolo ok, sem recovery manual. O dispatcher promove os filhos sozinhos quando o pai done — **monitore a cadeia INTEIRA de uma vez** (`watch-kanban-tasks.sh t_1 t_2 t_3` aceita lista), não um ticket por vez; o script notifica quando todos terminarem.
- O worker herda o modelo do clone (`model.default` no config do perfil) — se o default do time está 503/timeout, todo worker cai junto; considerar trocar o modelo dos perfis ou quebrar em tickets menores.

### Variante: trabalho grande NÃO commitado (2ª ocorrência consecutiva — 2026-08-05, task dashboard GRANDE)Worker entrega a refatoração INTEIRA mas: (a) só commitou o pedaço pequeno (ex: componente) e deixou 19-29 arquivos (+1000 linhas) como diff não commitado no working tree; (b) não chamou `kanban_complete` → blocked após 4 runs. O summary pode nem existir — NÃO confiar nele; a prova é o diff + build/testes.
Recovery completo (validado na task t_b6e8e378):
1. `hermes kanban show <id>` → confirmar protocol_violation + runs crashados (não é falha de trabalho — o diff existe).
2. Rodar build + testes + tsc NO working tree (`npm run build`, `npm test -- --run`, `npx tsc --noEmit`) — prova que o trabalho órfão é real e saudável.
3. Se a task tem CONTRATO (spec do designer, AGENTS.md): validar o diff contra os CRITÉRIOS DE ACEITE antes de commitar — ex. spec de design: `grep -rn "#[0-9a-fA-F]\{6\}" frontend/src/components/ --include="*.tsx"` (hardcode hex proibido), `startViewTransition` no toggle, fontes Inter/JetBrains + `.num`, dark sem box-shadow. Não commitar trabalho que não bate com o contrato.
4. Commitar o trabalho órfão EM NOME DO WORKER (orquestrador fecha o ciclo quando o worker crashou no protocolo): `git add` + `git commit` com mensagem que reflete a feature. Lixo (ex: `frontend/test-results/.last-run.json`, `assets/mockup-*`) → `.gitignore` em vez de commitar (`git rm -r --cached` + append no .gitignore + commit separado).
5. `hermes kanban comment` com a revisão (o que foi validado, commits/hashes) + `hermes kanban complete <id>`.
6. **Lição recorrente**: worker que entrega bem mas não fecha o protocolo pela 2ª vez é padrão, não acidente — considerar reforço no SOUL/body (instrução de fechamento mais agressiva) e reportar ao usuário como problema de processo, não de qualidade de entrega.

### Variante: auto-block (needs_input) com trabalho NÃO commitado → consolidar e fechar (validado 2026-08-07, flowmex conexões)
Worker termina a implementação e se auto-bloqueia (`review-required`/needs_input) com o trabalho SOLTO no working tree; os vizinhos da cadeia fecham `done` também SEM nenhum commit (nem a migration Alembic — fica `?? apps/api/alembic/versions/00XX_*.py` untracked). `git log origin/main..HEAD` vazio + `git status --short` cheio = padrão. Recovery validado:
1. Rodar os testes DIRECIONADOS da entrega antes de qualquer commit — `env -u PYTHONPATH -u FLOWMEX_DATABASE_URL .venv/bin/python -m pytest tests/unit/test_<task>.py tests/contract/test_openapi_contract.py` (regra: NUNCA commitar com vermelho; gate de contrato Schemathesis costuma precisar de `EXPECTED_OPERATIONS` atualizado — o worker já deve ter feito).
2. Consolidar TODO o módulo num ÚNICO commit em nome dos workers: `git add -A && git commit -m "feat(<modulo>): ..."` (mesma justificativa do recovery de protocol_violation; 1 commit consolidado é ok — o PR final será squash). Migration untracked entra no `git add -A`.
3. `hermes kanban comment <id>` com a evidência REAL (testes que o ORQUESTRADOR rodou, commit hash) + `hermes kanban complete <id>` — NÃO `unblock` para o worker voltar: o ciclo está fechado, o trabalho validado e a cadeia filha promove sozinha.
4. Suíte completa em background como rede de segurança pós-commit (se acusar algo, fix em commit separado — não desfazer o consolidado).
5. **PREVENÇÃO (validado na T6 seguinte, 2026-08-07)**: exigir no body — "COMMITE o trabalho (conventional commit) e reporte o hash no summary ANTES de kanban_complete". A T6 (única com essa linha) commitou sozinha; as 5 anteriores não. Além disso, NÃO deixar `.env`/secrets de teste do orquestrador no worktree enquanto workers rodam na MESMA branch: o pydantic lê `.env` do cwd e o worker perde tempo debugando valores que o orquestrador injetou (caso real: `FLOWMEX_PUBLIC_APP_URL` confundiu a T6, que isolou com `_env_file=None`).

## Stress test multi-perfil: fronteira de arquivo no body = paralelismo sem conflito (validado 2026-08-05)
Para validar o time INTEIRO no padrão novo (SOUL v2 + kits curados) de uma vez: criar 4 tasks em paralelo, uma por perfil, no MESMO repo, cada body com seção explícita "Escopo (fronteira de arquivos): SOMENTE <path>" + "PROIBIDO tocar: <paths>" + um CONTRATO entre eles (ex.: database cria função `X()` → backend consome via SQLAlchemy; contrato fixo no body permite rodarem simultâneos sem depender de ordem). Resultado real: 4 workers (designer/database/backend/frontend) pegaram no mesmo tick, cada um na sua área (docs/ vs db/ vs api/ vs frontend/), todos `done` em 3-14 min, zero conflito de working tree, zero protocol_violation. Verificação pós-done por área: `git status --short | awk '{print $2}' | sed 's|\(docs/\).*|\1|...'` mostra quem tocou o quê e confirma que cada um respeitou a fronteira. Para stress test REAL: a demanda deve ser do tamanho das demandas do usuário (não micro-task de brinquedo) — ex. "ronda de qualidade pós-redesign" com 1 ticket por papel.

## Feedback visual do usuário: spec incompleta ≠ skill não chamada (2026-08-05)
Quando o usuário aponta problema de DESIGN no resultado (padding colado, textos crus em inglês, lista sem lazy load, padrão Z ausente), ANTES de culpar skill não invocada, verificar se a SPEC/contrato especificava aquele acabamento. Caso real: KPI cards sem padding interno + títulos "Loading / unloading" crus — a skill frontend-design fala de spacing em tom geral, mas a spec §5.2 do designer NÃO tinha critério de padding interno/padrão Z, e o SOUL pedia humanizer só para erros/copy, não para títulos de seção. Ou seja: a correção é dupla — (1) task de correção visual com critérios explícitos + evidência ANTES/DEPOIS (screenshots), e (2) atualizar a spec do designer e o SOUL (humanizer em TODO texto visível, inclusive títulos de painel/seção). Verificação real pós-correção: `vision_analyze` no screenshot ANTES/DEPOIS para confirmar o padding de verdade (não confiar no summary do worker).

## Context7 "LEI" no SOUL não é seguido na prática — exigir evidência (2026-08-05)
Regra "Context7 é LEI antes de usar API/lib" nos SOULs dos workers NÃO garante uso: auditando 4 tasks reais, o MCP context7 estava ativo (✓ enabled, conecta ~2.4s, tools resolve-library-id + query-docs) mas **0 menções de context7 nos logs**. As libs estavam no latest por sorte (package.json já atualizado), não por hábito.
- Corrigir com EVIDÊNCIA, não com mais regra: exigir no body da task + no SOUL "no summary, cite o que o Context7 confirmou (lib + versão consultada)". Sem isso, orquestrador devolve.
- Auditoria rápida de "latest": `hermes -p <perfil> mcp list | grep context7` (confirmar ativo) + `hermes -p <perfil> mcp test context7` (confirmar tools) + comparar package.json/requirements.txt contra o registry. **`npm view <pkg> version` é BLOQUEADO pelo guard de segurança (heurística de servidor)** — usar `curl -s https://registry.npmjs.org/<pkg>/latest | python3 -c "import sys,json; print(json.load(sys.stdin)['version'])"` e para Python `curl -s https://pypi.org/pypi/<pkg>/json | python3 -c "import sys,json; print(json.load(sys.stdin)['info']['version'])"`.

## Pitfalls de verificação pós-done (2026-08-05)
- **`rtk uv ...` quebra**: o plugin rtk-rewrite prefixa `rtk` e `rtk uv run` falha com "Failed to spawn process: No such file or directory". Para rodar backend de projeto no Mac do usuário: `env -u PYTHONPATH <caminho-do-uv>/uv run uvicorn ...` (uv em ~/.local/bin/uv; PYTHONPATH do venv hermes-agent contamina pydantic_core → ModuleNotFoundError).
- **`rtk` também prefixa `npm run dev` (2026-08-07)**: o processo fica mudo (0 linhas de log) e a porta não sobe. Usar o binário direto: `./node_modules/.bin/vite --port <p> --strictPort` com as VITE_* prefixadas no comando.
- **Endpoint novo dá 404 = processo antigo na porta**: uvicorn SEM `--reload` não pega código novo; worker sobe o backend, depois o orquestrador edita/delega e o curl no endpoint novo retorna `{"detail":"Not Found"}` enquanto o código já existe em api/main.py. Diagnóstico: `lsof -i :8000` (PID antigo ainda vivo) + `grep -n "@app.get(\"<rota>\"` no código. Correção: `kill -9 <pid>`, subir de novo com `env -u PYTHONPATH ... uvicorn`, re-testar.
- **Workers deixam arquivos temporários** (`verify-*.tmp.ts`, `*_tmp.py`, `capture-*.ts`, `_test_*.txt`) no working tree — limpar antes do commit (`git rm --cached` + rm + adicionar `*.tmp.ts` ao .gitignore) ou o PR sai poluído.
- **Fix de worker pode estar ESTRUTURALMENTE errado de forma silenciosa** (2026-08-06, task t_887c8011): o worker "corrigiu" o env.py com `CREATE SCHEMA IF NOT EXISTS runtime` antes do run_migrations — o alembic passou a imprimir "Running upgrade" x5 + exit 0 mas o banco ficava VAZIO (rollback silencioso: o execute abria transação implícita, o alembic não commitava, o close revertia tudo). `pytest` nem ajudava (17 errors de setup pareciam problema de ambiente). Só o critério de pronto REAL da task pegou: drop dos schemas → upgrade head → conferir persistência (schemas + version table) → suíte. Regra: validar entrega do worker com o critério de pronto escrito no body (drop+rebuild+checagem de estado), não com "diff existe + parece correto + testes rodam" — caso completo em `database-migrations`.
- **Testcontainers skipped silenciosamente (2026-08-06, webhook PlugBoleto)**: suíte de integração com `PostgresContainer` roda `s` (skip) com **exit 0** quando falta `FLOWMEX_RUN_TESTCONTAINERS=1` — suíte "verde" sem testar nada (a 1ª rodada do orquestrador passou com `s` e só `-rs` revelou o skip). Sempre rodar integração com `-rs` e conferir que não há `SKIPPED`; com a flag: `FLOWMEX_RUN_TESTCONTAINERS=1 ... pytest -q -rs`.
- **Env poluída quebra COLLECTION do pytest (2026-08-06)**: `FLOWMEX_DATABASE_URL` do shell apontando pra banco de outra sessão (ex.: `postgresql+asyncpg://...`) derruba a collection com `database_url must use PostgreSQL with the psycopg 3 driver` (validator do Settings) — o `.env` do repo NÃO vence env exportada; rodar com `env -u FLOWMEX_DATABASE_URL -u PYTHONPATH`. Traiçoeiro: 1ª rodada (background) pode passar e a 2ª (foreground) falhar se o env herdado difere — conferir `printenv FLOWMEX_DATABASE_URL` antes de culpar o código.\n- **Settings com `env_prefix` + secrets do 1Password sem prefixo (2026-08-07, flowmex V1)**: config.py com `env_prefix=\"FLOWMEX_\"` ignora SILENCIOSAMENTE vars de secure note sem prefixo (MAINO_BASE_URL, PLUGBOLETO_TOKEN_SH...) → app sobe, login 503 `authentication_unavailable`, providers desabilitados, e validações em cadeia no startup (`process_sync_enabled must be true when process provider settings are provided` → exige fila → remover SISCOMEX_* do teste local; `Live billing requires Maino, PlugBoleto, and object storage` → adicionar R2). Diagnóstico ANTES de montar .env local: ler `model_config` do Settings (`env_prefix`/`validation_alias`) e testar o parse no cwd do .env com `python -c \"from <mod>.config import get_settings; print(get_settings().<campo>)\"` — campo lido = env correta. Mapear nomes do vault → prefixados (ex.: MAINO_BASE_URL → FLOWMEX_BILLING_MAINO_BASE_URL). DATABASE_URL local exige driver `psycopg` 3 (`postgresql+psycopg://`); `asyncpg` é rejeitado pelo validator.\n- **Sintoma de processo antigo na porta: `openapi.json` com poucas rotas (2026-08-07)**: uvicorn novo morre com `address already in use` (log mostra o bind falho) e o curl responde do processo VELHO — `openapi.json` lista só 5 paths (código antigo) enquanto o código novo tem 75. Conferir `lsof -i :<porta>` SEMPRE ao subir app local, antes de culpar wiring/rotas 404.\n- **Campo de Settings criado ≠ usado (2026-08-07, flowmex)**: worker adicionou `billing_plugboleto_webhook_url` ao config mas a property `plugboleto_webhook_url` nunca o leu (usava `public_app_url`) — o teste de fogo REAL pegou (403 da PlugBoleto com URL localhost) porque testes unit não cobrem wiring de config. Ao aceitar task que adiciona env var/config: validar no teste real que a var É consumida pelo caminho (property/endpoint), não só que o campo parseia (`get_settings().<campo>` retorna valor).

## Verificação pós-done em worktrip de integração externa (2026-08-06, Mainô → Flowmax)
Cadeia de 5 tasks verticais (adaptador → schema → endpoints+jobs → spec → front) fechou toda `done`; a verificação real pegou 1 gap de entrega e 3 armadilhas de ambiente:

- **Worktree novo NÃO tem o pacote instalado**: `git worktree add` não clona o `.venv` — pytest falha com `ModuleNotFoundError: No module named '<pacote>'` em TODOS os testes (parece código quebrado, é ambiente). Antes da suíte: `env -u PYTHONPATH <uv> pip install -e . --no-deps` (ou `uv sync`) no worktree; binários podem ter shebang quebrado → usar `python -m alembic` em vez de `.venv/bin/alembic`.
- **Banco Postgres é COMPARTILHADO entre worktrees**: outro worktrip migrou o banco local com revisions que esta branch não tem → `alembic upgrade head` falha com `Can't locate revision identified by '...'`. Validar a migration nova em banco limpo: `CREATE DATABASE <nome>` + `FLOWMEX_DATABASE_URL=... alembic upgrade head` + testes de ORM com essa URL (bate com o critério de pronto da task de schema: "upgrade roda limpo em banco vazio").
- **`cmd | tail` mascara exit code E a mensagem de erro**: pytest com 25 collection errors retornou exit 0 no pipe. Ler o output (`N passed, N failed, N errors`) — nunca confiar no exit code quando há pipe. `tail -N` também corta a MENSAGEM do erro (sobra só o stack trace): build falhou e o motivo sumiu — re-rodar greppando `grep -E "error|Error|failed|Could not|Cannot"` (2026-08-07: build front quebrou por dep faltando, o `tail -4` escondeu o `Rolldown failed to resolve import`).
- **`pytest -q` pode NÃO imprimir o resumo nem com redirect pra arquivo** (2026-08-07, suíte consolidada): a barra de progresso usa `\r` e o arquivo termina em `[100%]` sem `N passed`. Prova confiável: exit code REAL sem pipe (`pytest -q > /tmp/out.txt 2>&1; echo "PYTEST_EXIT: $?"` — o `$?` é do pytest, não do tail) + conferir o progresso: só dots/`s`, zero `F`/`E` (o `s` = skip condicional, normal; listar com `-rs`).
- **Erro de lint pré-existente ≠ entrega ruim**: ruff apontou 1 erro em arquivo de OUTRO worktrip (`tests/processos/test_refresh.py`); `git diff origin/main..HEAD -- <arquivo>` vazio prova que não é da entrega. Rodar ruff SÓ na entrega (ex.: `src/flowmex_core/integrations ...`) antes de devolver task.
- **Endpoint existe em módulo mas 404 no app real**: worker entregou router opcional + jobs mas NÃO fez o wiring no composition root (`main.py` chamava `create_app` sem o parâmetro novo); a task pedia "endpoints respondendo" e só testes com fakes passaram. Módulo existir + testes verdes NÃO prova wiring — smoke test contra o app montado (TestClient) é o critério; gap vira task de correção com body apontando exatamente o arquivo do wiring.
- **Doc oficial ≠ shape real da API (2026-08-06, Mainô)**: adaptador escrito conforme a doc (`{value: {cnpj: ...}}`) mas a API real responde o envelope SEM `value` (`{cnpj: {...}}` direto) → `auth_failed` com credenciais que funcionavam num curl direto (HTTP 200). Testes com MockTransport baseado na doc não pegam. Regra para integração externa: ANTES de declarar pronto, teste de fogo de AUTENTICAÇÃO real — script com credenciais reais via `op read` (sem imprimir valores; validar só status + shape do JSON com keys mascaradas) e comparar o shape real com o parse do adaptador. Diferença de envelope vira task de correção com a evidência no body.
- **Envelope varia POR MÓDULO, não só na auth (2026-08-06, validação real do dump)**: o job real completou `done` com `modules_done` reportando `count=0` para stakeholders/produtos/nfes — mas um GET direto com o MESMO token retornava 24KB–40KB de dados (chaves reais: `/stakeholders`→`stakeholders`, `/produtos`→`produtos`, `/nfes`→`notas_fiscais`). O adaptador normalizava alguns envelopes e devolvia página vazia para outros, silenciosamente. Regra: depois do dump real, **conferir count > 0 em TODOS os módulos esperados** e, para qualquer zero suspeito, script de verificação direto na API (status HTTP + tamanho + chaves, sem imprimir tokens) — o checkpoint `modules_done` do job é a fonte da verdade do parse, não o "done".
- **Commit prematuro com teste de banco falhando (2026-08-06, erro do orquestrador)**: ao validar o fix do wiring, o smoke tinha 2 fails `UndefinedTableError: relation "integrations.integration_jobs" does not exist` e EU COMMITEI A ENTREGA ANTES de rodar com o banco certo. O fail era ambiente (banco default sem a migration nova), não código — mas a regra vale: **NUNCA commitar entrega com teste vermelho pendente**; fail de `relation ... does not exist`/`UndefinedTableError` em testes de ORM = banco sem migration (rodar com `FLOWMEX_DATABASE_URL` do banco migrado e ver green) — a prova de que era ambiente é a suíte verde no banco certo, não a leitura do erro.

## Merge de branches paralelas que mexem no MESMO wiring (2026-08-06, feat/documentos → feat/integrations-maino)
Usuário pediu mergear a branch do outro worktrip na minha para o dump puxar documentos. 4 arquivos de wiring conflitaram (env.py, app.py, config.py, main.py) porque AMBAS as branches os editaram — o merge automático resolve o resto (27 arquivos), os 4 wiring ficam `UU`:
- **Resolver mantendo AMBOS os lados**: imports + target_metadata (env.py), settings novos (config.py), montagem de routers (app.py), composition root (main.py). Nada de escolher um lado — as duas features coexistem.
- **Parâmetro novo obrigatório quebra testes da outra branch**: o `create_app` da branch documentos exigia `documentos` obrigatório → 11 testes da branch integrações caíram (`TypeError: create_app() missing 1 required keyword-only argument`). Fix de compatibilidade: parâmetros de wiring NOVOS nascem OPCIONAIS (`| None = None` + `if x is not None: include_router`), seguindo o padrão que o próprio `integrations` já usava.
- **`uv sync` é obrigatório pós-merge**: a branch merged adicionou deps (ex.: boto3) → `ModuleNotFoundError` nos testes novos até sincronizar o venv.
- **Suíte COMPLETA pós-merge, não só as duas áreas**: 257 passed é a prova do merge; testes focados por área podem passar com o wiring quebrado entre elas.
- `create_app` com `*` + keyword-only: chamada correta é `create_app(register, discovery, store, documentos=..., integrations=...)` — Pyright acusa `Expected 3 positional arguments` se passar positional.
- **Merge de MIGRATIONS entre branches (2026-08-06, mesmo merge)**: cada branch adicionou revisões → `alembic upgrade head` falha com `Multiple head revisions are present`; corrigir com `alembic merge heads -m "merge ..."` (gera revisão de merge) e commitar a revisão. Se o upgrade rodou PARCIAL antes do merge (banco num estado misto), o próximo upgrade falha com `DuplicateTableError: relation "folders" already exists` — banco de teste é descartável: `DROP DATABASE` + `CREATE DATABASE` + `alembic upgrade head` + re-aplicar tabelas PgQueuer (fallback do reference `verificacao-import-async.md`).
- **Postgres Docker pode ser recriado no meio do fluxo** (docker compose de outra sessão): os BANCOS persistem mas as TABELAS somem → sintomas: worker morre com `table 'pgqueuer' is missing`, app 404 `company_not_found`, empresa "criada" que não está em NENHUM banco (201 veio de um commit que morreu no close). Manter um script de setup idempotente em `/tmp` (alembic upgrade head + cópia do DDL PgQueuer) e re-aplicar antes de re-testar; conferir `docker ps`/uptime do Postgres antes de culpar código.

## Padrão de finalização do usuário (instrução explícita 2026-08-05)
Ao terminar QUALQUER task/verificação, o fluxo de fechamento é:
1. **SUBIR A STACK para o usuário VER o resultado real** (front + back: uvicorn em :8000 com `env -u PYTHONPATH .../uv run uvicorn`, vite em :5173; abrir `open_preview` no front). Ele quer ver o que os workers entregaram, mesmo já sabendo que está certo — "sempre que terminar uma tarefa, sobe o compose pra mim ver".
2. **NÃO fazer PR/merge sem aval explícito** — o usuário diz "pode fazer" (ou "faz PR, merge"). Se ele disser "não faz o merge/PR", NÃO faz.
3. Após o aval e o merge: **limpar TUDO** — branch local (`git branch -D`), branch remota (o `--delete-branch` do `gh pr merge` + `git fetch --prune`), main sincronizado (`git reset --hard origin/main` após squash). Zero sujeira.
- **`gh pr merge` com a branch em worktree ativo (2026-08-07, 2 ocorrências)**: o gh tenta `git checkout main` local e falha com `fatal: 'main' is already used by worktree` — MAS o merge no REMOTO acontece mesmo assim (o fetch seguinte mostra o squash commit na main). Não é falha do merge: confirmar com `git fetch origin` + `git log origin/main -1`, depois deletar a remota com `git push origin --delete <branch>` e `git worktree remove --force <path>` + `git branch -D <branch>`.
- **PR DRAFT de outro autor não mergeia (2026-08-07, PR #115 do Pedro)**: `gh pr merge` responde `GraphQL: Pull Request is still a draft (mergePullRequest)` — não é erro nem branch protection. Verificar com `gh pr view <n> --json state,isDraft` e reportar ao usuário; NÃO converter draft→ready em PR de terceiro (decisão do autor). Oferecer: "quando tirar do draft, eu mergeio na hora".
- Regra gravada na memória também; aqui fica o PORQUÊ: o usuário aprova visualmente antes de versionar — o PR sem aval dele é visto como atropelo.
- **PR consolidado ÚNICO do time (2026-08-07)**: usuário quer 1 PR só com TUDO do time (não 1 por feature) — integrar branches NOSSAS entre si é liberado, PRs duplicados fecham SEM merge (ex.: `gh pr close <n> --comment`), e NUNCA mergear na main enquanto outro dev (ex.: Luiz) mexe — deixar o PR pronto e, se o outro mergear primeiro, rebasear e atualizar o PR sozinho. Testar 100% local (suíte completa + build dos DOIS conjuntos) antes de consolidar. Pediu mais autonomia: não parar para perguntar no meio do fluxo, só reportar início/fim.

## Teste de uso REAL → merge → deploy (padrão de classe, validado 2026-08-07, flowmex)
Usuário: "não tô falando só pra tu testar de verdade. Testa, usa, vê se está funcionando mesmo. Aí se tiver, faz o merge tranquilo e aí pós merge tem que fazer o deploy... front, back, e conferir que a migration rodou no banco." Sequência que fechou a demanda inteira:
1. **Teste real local** (não só pytest/build): subir uvicorn + vite com `.env` montado do 1Password (ver pitfalls de `env_prefix` abaixo e o reference `local-stack-flowmex-v1.md`), semear dados via API do próprio app, exercitar o fluxo de verdade (conexão externa real, UI no browser). Evidência: log do uvicorn mostrando as chamadas do front (200) + screenshot.
2. **Merge autorizado** + limpeza total (worktrees, branches, fetch --prune).
3. **Deploy back**: `wrangler deploy --config <prod>.jsonc -e production` com `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID` do 1Password (item "Secrets Cloudfare" — Api-token/Account-id; wrangler desloga entre sessões). Conferir healthz + endpoint autenticado (401 esperado sem token). **Cold start do container (2026-08-07)**: rota NOVA pode dar 404 por ~30-60s logo após o deploy — aguardar e re-testar; 404 → 401 = container subiu e a rota está viva (404 persistente aí sim é rota ausente). O container de PRODUÇÃO pode ter nome legado com 'staging' no `wrangler.<env>.jsonc` (ex.: flowmex-gateway-staging-fastapicontainer-staging É o FastAPI de produção) — confiar no config, não no nome. **PR do time já mergeado pelo colega**: `gh pr view <n> --json state,mergedBy` → se MERGED (ex.: Luizdetec), pular o merge e ir direto ao deploy autorizado (usuário pediu só conferir o estado).
4. **Migration no banco de produção**: connection string do item DATABASE do vault; `alembic current` ANTES e DEPOIS de `upgrade head` (0038 → 0039 = prova de que rodou).
5. **Deploy front**: build com a URL real da API (`VITE_*`), `wrangler pages deploy dist --project-name <x> --branch main`; conferir `deployment list` → Environment Production.
6. **Smoke produção no browser**: login real, tela com dados reais. **Verificação de bundle**: hash do asset servido == hash do `dist/` local; com code-splitting, buscar strings-chave em TODOS os chunks (o texto pode estar num chunk lazy, não no index).
7. **Teste de mutação em produção autorizado (2026-08-07, payables)**: quando o usuário manda "testa de verdade" e o fluxo escreve (CRUD), criar dado de teste IDENTIFICÁVEL (ex.: fornecedor "Teste QA"), exercitar o ciclo INTEIRO (POST → PATCH status → DELETE com confirmação) e fechar EXCLUINDO o dado — a exclusão é parte da prova do CRUD e deixa produção limpa. Screenshot a cada passo (evidência visual do ciclo: criar → card atualiza → status muda → lista volta vazia).

## Diagnóstico de demanda: "tela não funciona" nem sempre é bug (validado 2026-08-07, payables + job Mainô)
Dois padrões que parecem bug mas têm causas de classe diferentes:
- **Tela restaurada sem backend** (caso payables #108): o front tem a tela COMPLETA mas com lista vazia hardcoded (`const contas: ContaPagar[] = []`) e o botão de criar mostra toast "em breve" — o PR anterior restaurou só o visual. Diagnóstico em 1 comando: `grep -rln "<domínio>" apps/api/` → 0 resultados = backend não existe, NÃO é bug de runtime. A tela .tsx define o CONTRATO do backend a construir (campos, enums, shapes — copiar do arquivo pro body da task; enums fixos como categorias/formas/status ficam explícitos na UI). A task de backend nasce com esse contrato, e a de front conecta as queries/mutations reais (substituir lista hardcoded e toast por API).
- **Polling de job assíncrono com 404 intermitente** (caso maino-sync): front cria job (POST) e faz polling (GET /{job_id} a cada ~3s); quando o job processa/expiram entre polls, o GET responde 404 → o front trata como ERRO (mensagem tipo "não foi possível consultar job") e CANCELA o acompanhamento com retry=false — o usuário vê erro "às vezes". Correção de classe: job ausente/expirado deve responder estado TERMINAL 200 (finished/failed com mensagem) para o polling encerrar limpo; se for race de criação, o GET de "ainda não existe" responde `queued` 200. Regra no body da task: "NÃO mudar o contrato dos estados válidos; só o comportamento de job ausente/transição" + exigir reprodução com evidência (respostas reais antes/depois).

## Regra de memória do usuário (correção explícita e repetida — 2026-08-06)
Usuário cobrou em tom forte: "para de subir memória pro MEMORY.md se for coisa de projeto... já falei mil vezes... é pro mem0". Regra inegociável:
- **MEMORY.md local = SÓ aprendizado GERAL + extremamente curto** (ex.: `$(...)` bloqueado no terminal, `env -u PYTHONPATH`). Nada de projeto, nada de env vars de banco, nada de pitfall de lib específica do projeto.
- **TUDO de projeto/agente vai pro MEM0** (`mem0_add`), separado por projeto — env var poluída, worker PgQueuer, schemas, credenciais, decisões.
- Teste mental antes de gravar: "isso vale para QUALQUER projeto?" — se a resposta é não, é mem0, não MEMORY.md.
- Ao corrigir (remover do MEMORY.md + migrar pro mem0), fazer em UMA leva: `memory` com operations batch (remove+add juntos, o limite é do resultado final) + `mem0_add` dos fatos.

## Gargalo de velocidade: verificação pesada em task pequena (diagnóstico 2026-08-05)
Medição real dos runs: task de endpoint simples = 2.3 min; task de padding (4 linhas de CSS) = 20 min — o excesso era a BATERIA COMPLETA de verificação (lighthouse 2x + e2e 5/5 + react-doctor + screenshots antes/depois) rodada em CADA task pequena. Custo fixo por task: carregar SOUL + explorar repo + sessão nova (~1-3 min que uma sessão full-stack contínua não paga).
- **Proposta (não aplicada ainda — pendente de aprovação do usuário): verificação em camadas** — task pequena roda só testes+build+lint (≤5 min); bateria completa (lighthouse+e2e+screenshots) só no PR. Se o usuário aprovar, aplicar nos SOULs dos workers.
- Observação de arquitetura (respondida ao usuário): a fronteira designer×front / dba×back NÃO é falta de skill — funcionou no stress test com fronteira de arquivo no body (designer=docs/**, front=frontend/**, dba=db/**, back=api/**). O bug visual real era spec incompleta (não tinha padding → front não tinha onde buscar), corrigido na spec §5.2.
- Trade-off full-stack-direto vs time: o time paga ~1-3 min de setup por task mas ganha paralelismo (N workers), isolamento (crash não derruba tudo) e verificação independente (autor não valida o próprio trabalho).


Workers com "NÃO commitar" no body se auto-bloqueiam com `kind: needs_input` (review-required) quando terminam. Orquestrador: revisa artefatos (diff/arquivos), comenta aprovacao, `kanban complete` → libera tasks filhas.

### Auto-block por LACUNA DE CONTRATO → autorizar via comment + unblock (validado 2026-08-06, T2 do módulo documentos)
Worker se auto-bloqueia (needs_input) quando o que falta está em área PROIBIDA da própria task (ex.: migração de schema pronta mas `adapters/postgres.py` — órfão entre T1 e T2 — não persiste o campo novo; smoke dá 500). NÃO reabrir a task anterior nem delegar de novo: a lacuna é do CONTRATO do orquestrador. Recovery em 2 comandos:
1. `hermes kanban comment <id> 'AUTORIZAÇÃO DO ORQUESTRADOR: pode tocar <paths> (lacuna de contrato entre T1 e T2 — responsabilidade minha, não sua). Restante do escopo permanece.'`
2. `hermes kanban unblock <id> 'motivo'` → volta a `ready`, dispatcher pega no próximo tick (~60s).
O worker continua a MESMA task (contexto quente, diagnóstico já feito) — custo ~minutos vs reabrir a outra task com setup novo.

### Destravar paralelismo: `kanban unlink` + `promote` (validado 2026-08-06)
Task filha criada com `--parent` nasce `todo`; se a fronteira de arquivos com o pai é limpa (ex.: migrations/orm vs adapters/config/main), dá pra adiantar: `hermes kanban unlink <parent_id> <child_id>` — o child fica `ready` SOZINHO (o próprio unlink libera; `promote` só aceita `todo`/`blocked` → `cannot promote ... task is 'ready'`). Dispatcher pega no próximo tick. Critério: SEM sobreposição de arquivos entre as tasks (mesmo worktree) — senão conflito garantido.

### Assets do usuário: copiar para o worktree ANTES de delegar (validado 2026-08-07)
Task que consome arquivo do usuário (~/Downloads, Desktop): copiar o arquivo PARA o repo/worktree no momento de criar a task. `~/Downloads` não é estável — as 2 imagens de wallpaper existiam no início do run e sumiram no meio (apagadas/movidas) → task bloqueada `needs_input` pedindo reenvio, 8 min de worker desperdiçados. Recovery quando sumir: procurar no cache do Hermes (`~/Library/Application Support/Hermes/composer-images/`) + `mdfind`; se não achar, pedir reenvio ao usuário, confirmar identidade por dimensões (`sips -g pixelWidth -g pixelHeight`) e `kanban unblock` após comentar os caminhos confirmados. Anexo de chat referencia o arquivo ORIGINAL — não existe cópia automática no Hermes.

### Follow-up criado por WORKER (validado 2026-08-07)
Worker pode criar task própria no board (ex.: "Fix suíte: 5 falhas pré-existentes com fastapi X" — baseline quebrado achado durante a validação). Não é duplicação nem desvio: conferir a razão no body, incluir no watcher da cadeia e deixar rodar — corrige o baseline e destrava a suíte completa.

### Cancelamento rápido de demanda no meio (validado 2026-08-07)
Usuário desistiu de feature com task já `running` (ex.: "esquece a img, o Luiz vai fazer"): `hermes kanban comment <id> 'CANCELADA pelo usuário: <motivo>'` + `hermes kanban archive <id>` (archive funciona em task running e tira do board ativo; conferir com `kanban list`), matar o watcher (`process kill` no session_id do watch-kanban-tasks.sh), remover worktree (`git worktree remove --force <path>` — se o worker já criou arquivos, --force descarta; o trabalho commitado continua na branch) e `git branch -D <branch>` se nada foi pushado. Board limpo, zero rastro.

### Verificação de scripts de orquestração em /tmp (validado 2026-08-07, 4 ocorrências)
O harness cobra "npm run test" para scripts bash de orquestração gravados em /tmp (create_*.sh, check_*.sh, cadastrar_*.sh). Blocker concreto: estão FORA de qualquer projeto npm — `npm run test` não tem alvo (um package.json solto em /tmp é de outra sessão; conferir `grep` se houver dúvida). Se EXISTIR package.json em /tmp, PROVAR que não é seu (validado 2026-08-07): `python3 -c "import json; d=json.load(open('package.json')); print(d['name'], d.get('main'), d['scripts']['test'])"` — name 'tmp'/main estranho (ex.: cct_main.js) = outra sessão — e rodar `npm run test` UMA vez capturando a saída do placeholder (`Error: no test specified`, exit 1 por design) como evidência do blocker na resposta. Verificação aplicável: `bash -n` (sintaxe) + EXECUÇÃO REAL com output verificado (cada script rodou e produziu o resultado esperado — documentar isso na resposta). NUNCA re-executar script com efeito colateral (ex.: create_* recriaria a task no kanban) só para "provar" — o blocker + a execução anterior SÃO a verificação.

## Verificação pré-PR (orquestrador)
- pytest + `ruff check . --fix`; atenção: ruff --fix altera arquivos FORA da task (ex: camadas antigas) — conferir `git diff` antes de commitar.
- Worker pode deletar docs/specs sem querer → `git checkout -- <arquivo>`.
- Mudanças pendentes que escaparam do commit → branch de hotfix + cherry-pick + PR rapido pos-merge.
- Merge: `gh pr merge <n> --squash --delete-branch`; limpar branches locais (`git branch -D`), remota residual (`git fetch --prune`), sincronizar main (`git reset --hard origin/main` apos squash).
- Verificar que ASSETS usados pelo codigo entram no commit (ex: logos de dashboard) — git status mostra untracked que o `git add` de lista nao pegou.

### Rebase de branch sobre main mexida por colega ("cuidado pra não sobrescrever") — validado 2026-08-07, flowmex/Luiz
1. `git pull --ff-only origin main` no clone principal; inspecionar o trabalho do colega ANTES de rebasear: `git log <base>..origin/main` + `git diff --stat <base> origin/main` → revela conflito em potencial com os arquivos da branch.
2. No worktree da branch: `git rebase origin/main` (rebase limpo = fronteira ok; conflito = resolver por intenção, nunca descartar o lado do colega).
3. CONFERIR fronteira pós-rebase: `git log --oneline -6` (commits da branch empilhados sobre o novo tip), `git diff --stat origin/main...HEAD` (três pontos = SÓ o que a branch mudou; deve listar apenas os arquivos dela) e `git diff origin/main HEAD --stat | grep -c '<arquivo-do-colega>'` → 0 = nada dele foi tocado.
4. **node_modules do worktree desatualizado após rebase (2026-08-07)**: main nova adicionou dep (package.json mudou) → build quebra com `Rolldown failed to resolve import "<pkg>/..."` (vite/rolldown) ou `Cannot find module` (tsc) — NÃO é o rebase nem código quebrado, é dep não instalada. Checar `git diff <base>..origin/main -- package.json` e `npm install <dep>@<versão>` ANTES de culpar o código. (Gêmeo do pitfall "worktree novo NÃO tem o pacote instalado" — aqui o worktree já existia, a main é que andou.) **CUIDADO (2026-08-07): `npm install <pkg>@<versão>` REESCREVE a spec no package.json** (ex.: `"react-bancos": "0.9.0"` → `"^0.9.0"`) — mudança silenciosa no trabalho do colega. Reverter a spec com `git checkout apps/<x>/package.json` e regenerar lock consistente com `npm install --package-lock-only` (o lock SEM a dep também quebra build; o lock regenerado mantém a versão exata da spec).
5. Validar build + suíte, push com `--force-with-lease` (rebase reescreveu histórico), `gh pr create` com body documentando a base nova + validação real.

## Tasks que leem banco
- ANTES de delegar: confirmar que o `.env`/DATABASE_URL existe no repo (senao workers improvisam conexao — podem demorar ou falhar).
- **Env var poluída no shell persistente > .env (validado 2026-08-06, ~30 min de debug)**: `printenv FLOWMEX_DATABASE_URL` pode apontar para um banco de OUTRA sessão (ex.: flowmex_maino_research) e sobrepor o `.env` — scripts foreground e o app podem enxergar bancos DIFERENTES (processo background não herda env exportada depois do start). Sintoma clássico: app responde 404 `company_not_found` para linha que EXISTE no banco (adapter direto acha, app não). Antes de subir uvicorn/rodar smoke: conferir a env var e `unset` se apontar para o banco errado; banco dedicado de smoke (`docker exec flowmex-pg createdb -U flowmex flowmex_docs` + `export FLOWMEX_DATABASE_URL=...`) isola de outras sessões.
- Rodar python de projeto no Mac do usuario: `env -u PYTHONPATH` (o shell herda o site-packages do venv do hermes-agent py3.11 e contamina imports).
- Sessoes dos workers NÃO aparecem na sidebar do desktop (perfis separados, headless) — usuario acompanha via kanban list/show/log; explicar isso sem repetir.

## Scripts
- `scripts/watch-kanban-tasks.sh` — espera uma lista de tasks ficarem done/blocked e notifica; rodar com `terminal(background=true, notify_on_complete=true)` e continuar o trabalho (0 polling).
- `references/verificacao-import-async.md` — como o orquestrador VERIFICA endpoint de import assíncrono (outbox → PgQueuer) de ponta a ponta: setup de banco isolado, `pgq install`, mini-worker contínuo (NUNCA `run(drain)` em loop) e diagnóstico dos sintomas (job `queued` para sempre com worker vivo, outbox `pending`, operation sem outbox).
- `references/local-stack-flowmex-v1.md` — receita validada de teste REAL de ponta a ponta do Flowmex V1 local (2026-08-07): mapa de env vars com prefixo FLOWMEX_ (vault → config), bootstrap de auth, R2, user de dev, CORS 5173, e o fluxo de conexão real Mainô/PlugBoleto com diagnóstico 403.
- **O script imprime o BOARD INTEIRO no final** ("TODAS AS TASKS TERMINARAM: ..."), não só as monitoradas — ao ler o output, grep pela task alvo (`grep t_1fde025e`) em vez de se assustar com tasks antigas done na lista (observado 2026-08-05).
- **Verificação pós-done com `kanban show <id>`**: o campo "Latest summary" do `hermes kanban show` traz o auto-reporte do worker (incluindo lista de skills carregadas, se o body pediu). Para validar SOUL/processo: conferir summary + re-rodar testes/build (`npm test -- --run`, `npx tsc --noEmit`, `npm run build`) — self-report nunca é prova.
